<Window x:Class="KeepYourTime.ViewWindows.MessageWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MessageWindow" Height="150" Width="310"
        WindowStartupLocation="Manual" WindowStyle="None" AllowsTransparency="True">
    <Grid>
        <!--<Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>-->
        <Grid Name="ContentGrid">
            <Rectangle Fill="Teal"/>
            <StackPanel >
                <TextBlock Name="lblResult" Text="{Binding Status}"/>
                <TextBlock Name="lblMensagem" Text="{Binding Message}" />
                <TextBlock Name="lblQuery" Text="{Binding Query}" />
                <TextBlock Name="lblDetalhe" Text="{Binding StackTrace}"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
